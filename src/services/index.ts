import { ReactiveState } from './reactive.state';
import { ImmutableState } from './immutable.state';

export const APP_SERVICES: any[] = [
  ReactiveState,
  ImmutableState
];

export { ReactiveState } from './reactive.state';
export { ImmutableState } from './immutable.state';
